{
  "name": "taimio-key-service",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:init": "wrangler d1 execute taimio-keys --local --file=schema.sql",
    "d1:seed": "wrangler d1 execute taimio-keys --local --file=issued/seed.sql",
    "issue": "node scripts/issue.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.28.0"
  }
}
